name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Codegen (OpenAPI → hooks + Zod)
        run: pnpm --filter @workspace/api-spec run codegen

      - name: Build frontend
        env:
          NODE_ENV: production
          PORT: '3000'
          # GitHub Pages serves from the repo root by default.
          # If your repo is username/hook (not username.github.io), change to /hook/
          BASE_PATH: /
          # The deployed Replit API server that backs this static frontend.
          # All API calls (products, looks, setups, auth) go to this URL.
          VITE_API_BASE_URL: https://fashion-hook.replit.app
        run: pnpm --filter @workspace/hook run build

      # GitHub Pages needs a 404.html = index.html so SPA client-side routing works.
      - name: Copy index.html → 404.html for SPA routing
        run: cp artifacts/hook/dist/public/index.html artifacts/hook/dist/public/404.html

      # Prevent Jekyll from ignoring files that start with underscore.
      - name: Add .nojekyll
        run: touch artifacts/hook/dist/public/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: artifacts/hook/dist/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
